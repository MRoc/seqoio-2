extends layout.pug

block title
    title Sign-up for an Seqoio account

block content
    h2(class="block is-flex is-justify-content-center") Sign up for your account
    if message && message.length > 0
        div(class="has-text-danger block is-flex is-justify-content-center") #{message}
    form(class = "block" action = "/authentication/email/signup" method = "POST")
        input(name = "email" type = "text" required placeholder = "Email" class="input block is-fullwidth")
        button(type = "Submit" class="button block is-fullwidth") Sign-up
    hr
    div(class = "block is-flex is-justify-content-center")
            a(href='/authentication/signin') Already have an account?
